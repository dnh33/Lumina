# dependencies
node_modules/

# builds
dist/
*.tsbuildinfo

# local data — your private library, chat history and watch sources live
# here; sources.local.json is private config and must never be committed
# (data/* not data/ so the committed example below can be re-included)
data/*
!data/sources.local.example.json

# secrets
.env
.env.local

# logs & junk
*.log
npm-debug.log*
.DS_Store
Thumbs.db
get-art.mjs
